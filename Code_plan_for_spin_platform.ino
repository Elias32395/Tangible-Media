

void setup() {
  // put your setup code here, to run once:
//declare ints on both programs
// - led, button, RF transmitters,accelerometer and DC motor

}

void loop() {
  // put your main code here, to run repeatedly:
 //(sender side)
 //read data from accelerometer and button
 // send data to reciever

 //(reciever side)
 // if button is pressed, show led 
 // read data from sender
 // if x = (accelerometer data) then move dc motor 90 degrees to the right
 // if x = (accelerometer data) then move dc motor 90 degrees to the left
// if y = 
}
